# 校园闲置物品互助交换平台项目需求说明书

> 下面全部复制直接粘贴丢进 TraeAI 即可，专门适配 AI 代码生成，明确：做什么、不做什么、技术栈、数据库、接口、降级方案，禁止 AI 额外脑补多余功能。

```markdown
# 【TraeAI直接读取】校园闲置物品互助交换平台 项目需求说明书
## 任务指令
你作为后端工程师，严格按照本文档全部要求生成SpringBoot后端代码 + 简单前端页面，**禁止新增文档没有提到的任何功能，禁止自作主张增加图片上传、评论、收藏、支付模块**。
开发背景：3人小组课堂实训项目，总工期3天。AI助手是加分可选模块，必须支持开关降级。

## 1 项目概述
面向在校大学生的物物交换平台，不做金钱交易。
角色分为普通学生用户；管理员不开发前端页面，直接操作数据库完成管理。
学生可以注册登录、发布闲置物品、浏览物品、按分类筛选、发起交换申请；集成简易AI业务助手，可润色物品描述、回答平台操作问题，保存对话历史。
AI模块支持开关：ai.enable=true启用真实大模型API；ai.enable=false返回模拟回答，保证主业务完全不受第三方API故障影响。

## 2 技术栈
后端：
- SpringBoot 3
- Maven
- MyBatis 
- MySQL 8
- mysql‑connector‑j 8.0.33
- Spring‑AI 
- JWT简单登录鉴权
数据库库名：bruce_db

前端：简单HTML+JavaScript，可以用Vue全家桶，。

运行环境：JDK17，Windows。

## 3 用户角色
1. 普通学生用户：注册、登录、浏览物品、发布闲置、发起交换申请、查看申请记录、使用AI助手
2. 管理员：无前端页面，直接操作MySQL数据库修改数据

## 4 必须实现的核心功能（必做，全部完成）
1. 用户模块
- 用户注册：账号用户名唯一，密码存储，返回提示
- 用户登录：校验账号密码，返回JWT‑token，后续接口携带token鉴权
2. 物品分类模块
- 查询全部分类，初始化4条基础数据：教材书籍、数码产品、运动器材、生活用品
3. 闲置物品模块
- 发布闲置物品：关联分类id、填写物品名称、描述、期望交换物品
- 分页查询闲置物品列表，支持根据分类id筛选
- 下架自己发布的闲置物品
4. 交换申请模块
- 登录用户对目标物品发起交换申请，填写留言
- 查询我发起的全部申请记录
- 处理收到的申请：同意 / 拒绝，修改申请状态

## 5 可选加分功能（AI助手，必须实现降级逻辑）
AI助手功能：
1. 用户输入文字，AI可以润色闲置物品描述；回答平台操作相关问题；拒绝回答无关问题
2. 保存每一条用户提问、AI回复到ai_chat_record表
3. 查询当前登录用户历史对话记录
4. 配置项 ai.enable：true=真实调用通义千问API；false=返回预设模拟文本，不调用外部接口，项目不能报错。

## 6 ❌绝对不要实现（禁止生成以下代码）
- 收藏功能
- 图片上传
- 评论、留言板
- 支付、交易、金额相关逻辑
- 管理员前端管理页面
- 复杂流式AI输出，全部同步返回完整字符串

## 7 数据库表完整SQL，直接执行初始化
```sql
-- 用户表 sys_user
CREATE TABLE `sys_user` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '主键',
  `username` varchar(50) NOT NULL COMMENT '账号',
  `password` varchar(100) NOT NULL COMMENT '密码',
  `real_name` varchar(30) DEFAULT NULL COMMENT '真实姓名',
  `phone` varchar(11) DEFAULT NULL COMMENT '手机号',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_username` (`username`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户表';

--物品分类表 item_category
CREATE TABLE `item_category` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '主键',
  `category_name` varchar(64) NOT NULL COMMENT '分类名称',
  `sort` int DEFAULT 0 COMMENT '排序',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_category_name` (`category_name`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='物品分类表';

--闲置物品表 idle_item
CREATE TABLE `idle_item` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '主键',
  `user_id` bigint NOT NULL COMMENT '发布者用户id',
  `category_id` bigint NOT NULL COMMENT '分类id',
  `item_name` varchar(100) NOT NULL COMMENT '物品名称',
  `item_desc` text COMMENT '物品描述、成色',
  `hope_exchange` varchar(200) DEFAULT NULL COMMENT '希望交换什么物品',
  `status` tinyint DEFAULT 1 COMMENT '1正常上架，2已交换完成，3下架',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='闲置物品表';

--交换申请表 exchange_apply
CREATE TABLE `exchange_apply` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `apply_user_id` bigint NOT NULL COMMENT '申请人id',
  `item_id` bigint NOT NULL COMMENT '目标闲置物品id',
  `message` varchar(255) DEFAULT NULL COMMENT '申请留言',
  `apply_status` tinyint DEFAULT 0 COMMENT '0待处理 1同意 2拒绝',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='交换申请表';

--AI对话记录表 ai_chat_record
CREATE TABLE `ai_chat_record` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `user_id` bigint NOT NULL COMMENT '提问用户ID',
  `user_content` text NOT NULL COMMENT '用户提问内容',
  `ai_content` text NOT NULL COMMENT 'AI返回回答',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='AI聊天记录';

--初始化分类数据
INSERT INTO item_category(category_name,sort) VALUES
('教材书籍',1),
('数码产品',2),
('运动器材',3),
('生活用品',4);
```

## 8 API 接口清单，严格按此路径，不许修改路径

### 用户模块

POST /api/user/register 用户注册
POST /api/user/login 用户登录，返回 token

### 分类模块

GET /api/category/list 查询全部物品分类

### 闲置物品模块

POST /api/item/add 发布闲置物品
GET /api/item/page 分页查询闲置物品，参数：pageNum,pageSize,categoryId \(可选\)
PUT /api/item/off/\{id\} 下架物品

### 交换申请模块

POST /api/exchange/apply 发起交换申请
GET /api/exchange/my 查询我的申请记录
PUT /api/exchange/handle 处理申请，参数 id、apply\_status

### AI 助手模块（可选）

POST /api/ai/chat AI 对话接口
GET /api/ai/record/list 获取当前用户历史聊天记录

## 9 application\.yml 配置要求

```yaml
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/bruce_db?useUnicode=true&characterEncoding=utf8mb4&serverTimezone=Asia/Shanghai
    username: root
    password: 你的mysql密码
  ai:
    dashscope:
      api-key: 填写自己的通义千问key
#自定义开关 true真实调用AI false返回模拟回答
ai:
  enable: true
```

AI 系统提示词固定："你是校园闲置物品交换平台助手，只回答物品发布、交换相关问题，可以帮助润色闲置物品描述，拒绝回答无关话题。"

## 10 代码约束

1. 后端分层：controller、service、mapper、entity、config
2. 需要登录的接口做 token 鉴权拦截，未登录返回提示
3. AI 模块必须读取 ai\.enable 配置；false 不走外部 API，返回模拟文本，不能抛出异常
4. 统一返回结果封装类 Result，包含 code、msg、data
5. MyBatis 写 mapper xml 文件，不要全注解
6. 全局异常处理器，捕获异常返回友好提示

## 11 前端页面清单（简单 html）

1. 登录注册页面
2. 首页：闲置物品列表，分页、分类筛选
3. 发布闲置物品表单页面
4. 个人中心：我的发布、我的交换申请记录
5. AI 助手弹窗聊天界面

## 12 验收标准

1. 运行 SQL 脚本后数据库表和初始化数据完整
2. Postman 全部接口可以跑通
3. ai\.enable=false，项目正常运行，AI 返回模拟回答，不报错
4. 所有必做功能全部实现，不存在文档之外的多余功能
5. 前端页面完成基础交互，对接后端接口

## 13 输出交付物

1. 完整 SpringBoot 项目全部代码
2. pom\.xml 完整依赖
3. application\.yml 配置文件
4. 数据库初始化 SQL
5. 简单 README，说明项目如何启动

```Plain
---
### 使用提示
直接全部复制粘贴到TraeAI输入框，它会自动解析这份PRD生成整套项目。
> 如果Trae输出内容太多，你可以分阶段：第一次只生成后端代码，再生成前端。
> 当你需要，我可以再给你一份3人小组实训日志文本。
```

> （注：部分内容可能由 AI 生成）
