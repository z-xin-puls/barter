# 校园闲置物品互助交换平台 项目需求说明书（当前实现版）

> 本文档与项目代码保持同步，描述当前已实现的全部功能、数据库设计与接口清单。项目基于原课堂实训 PRD经多轮迭代扩展而成。

## 1 项目概述

面向在校大学生的**物物交换平台**，不做金钱交易。学生可以注册登录、发布闲置物品（需管理员审核）、浏览搜索物品、发起交换申请、与对方实时聊天沟通、约定交易时间地点（双方确认）、完成交换后互评。管理员使用独立登录入口，负责用户管理、物品审核、举报处理和数据统计。集成 DeepSeek AI 助手，可润色物品描述、回答平台操作问题，支持开关降级。

## 2 技术栈

| 类别 | 技术 |
|------|------|
| 后端 | SpringBoot 3.2.5、MyBatis(XML)、Maven |
| 数据库 | MySQL 8（库名 `barter_db`） |
| 安全 | JWT 鉴权（jjwt 0.11.5）、BCrypt 密码加密 |
| AI | DeepSeek API（`ai.enable` 开关降级） |
| 前端 | Vue 3（本地 js/vue.global.js）+ 原生 HTML/CSS |
| 环境 | JDK 17、Windows、端口 8081 |

## 3 用户角色

|角色|入口|职责|
|------|------|------|
| 普通学生用户 | `login.html`（含管理员入口链接） | 注册登录、发布/浏览物品、发起交换、聊天、评价、举报、收藏 |
| 管理员 | `admin-login.html`（独立登录页，独立接口 `/api/admin/login`） | 用户封禁/重置密码、**物品审核**、上下架、举报处理、数据统计 |

**安全设计：** 用户与管理员分表存储（`sys_user` / `admin_user`），登录接口分离，JWT 携带 `userType` 区分，管理端由 `AdminAuthInterceptor` 独立鉴权；密码 BCrypt 加密；`Authorization: Bearer <token>` 标准鉴权头。

## 4 功能清单

### 4.1 用户模块
- 注册：账号唯一，填写学号、院系、真实姓名、手机号
- 登录：校验 BCrypt 密码，检查封禁状态，返回 JWT token
- 忘记密码：输入账号重置为 `123456`（课堂简化实现，免登录）
- 用户字段：头像 avatar、状态 status（1正常/0封禁）

### 4.2 物品模块
- 发布：名称、描述、分类、成色（全新/轻微使用/明显使用）、校区/地点、期望交换、**图片文件上传**（`POST /api/upload/image`，≤5MB，jpg/png/gif/webp，按日期分目录存储，最多5张）
- **审核机制**：发布后 `audit_status=0 待审核`，**不出现在市场**；管理员通过（=1）后才上架展示，驳回（=2）附备注；用户可在"我的发布"查看审核状态
- 浏览：分页查询支持**关键词**、分类、成色、校区筛选；详情页自动累加浏览量 viewCount
- 下架：仅物品发布者可操作

### 4.3 交换申请模块（状态机）
```
0待处理 → (发布者)1已同意 / 2已拒绝
1已同意 → 双方各自"我确认完成"（apply_confirmed / owner_confirmed 各自标记）
        → 双方都确认 → 3已完成 + 物品状态=2已交换 → 双方可互评
```
- 申请时记录 `owner_user_id`（冗余发布者ID），收到的申请无需 JOIN item 表
- 双方信息（用户名/头像/学号）通过 VO 关联返回

### 4.4 交易时间地点（确认制）
- 一方填写 `trade_time` / `trade_location` → `trade_status=1 待对方确认`
- 对方可**同意**（→`trade_status=2` 双方已确认，锁定不可再改）或**反对**（填写自己的方案，填写方切换，回到对方确认，可循环直到达成一致）
- 填写方本人不能确认自己的方案；每次动作推送站内通知

### 4.5 用户聊天
- 关联交换申请的会话（`chat_message`：sender/receiver/apply_id）
- 气泡式聊天弹窗（my.html 内），历史记录加载、5秒轮询新消息、查看自动已读、未读数统计

### 4.6 消息通知
- 触发点：收到申请、同意/拒绝、交易信息待确认、对方确认完成、交换完成
- 接口：列表、未读数、单条已读、全部已读

### 4.7 评价 / 举报 / 收藏
- 评价：交换完成后双方互评（1-5星 + 文字），支持查用户评价列表和平均分
- 举报：举报违规物品/用户，管理后台处理（处理/驳回）
- 收藏：收藏/取消收藏、我的收藏列表、物品卡片实时收藏状态

### 4.8 AI 助手（降级设计）
- `ai.enable=true`：真实调用 DeepSeek（环境变量 `DEEPSEEK_API_KEY` 注入）
- `ai.enable=false`：返回本地模拟回答，不依赖外部 API，不报错
- 每轮问答保存 `ai_chat_record`，可查历史；RestTemplate 声明为 Bean 并配置超时

### 4.9 管理后台（admin.html）
- 数据统计（全部 COUNT 查询，含待审核物品数）
- 用户管理：列表、删除、重置密码（BCrypt）、封禁/解封
- 物品管理：列表、**详情弹窗（含图片/发布者学号院系）**、**审核通过/驳回**、上下架、删除
- 申请列表、举报管理

## 5 数据库设计（11张表）

| 表名 | 说明 | 关键字段 |
|------|------|---------|
| sys_user | 用户表 | student_no, department, avatar, status（无 role） |
| admin_user | 管理员表（独立） | username, password, real_name, status |
| item_category | 分类表 | icon, parent_id（支持二级分类） |
| idle_item | 物品表 | images(多图), item_condition, campus, view_count, audit_status, audit_remark |
| exchange_apply | 交换申请表 | owner_user_id, trade_time, trade_location, trade_confirmer, trade_status, apply_confirmed, owner_confirmed, apply_status |
| chat_message | 聊天消息表 | sender_id, receiver_id, apply_id, content, is_read |
| notification | 站内通知表 | user_id, type, title, content, related_id, is_read |
| review | 评价表 | apply_id, from_user_id, to_user_id, rating(1-5), content |
| report | 举报表 | reporter_id, target_type, target_id, reason, status |
| favorite | 收藏表 | user_id, item_id（联合唯一） |
| ai_chat_record | AI 对话记录表 | user_id, user_content, ai_content |

**SQL 文件规范：** 数据库更新只新增增量迁移文件，不修改 init.sql：
```
sql/init.sql                              # 10张表 + 初始分类/测试物品
sql/migration_20260915_chat_and_trade.sql # chat_message 表 + 交易时间地点字段
sql/migration_20260915_dual_confirm.sql   # 双方确认完成字段
sql/migration_20260915_trade_confirm.sql  # 交易信息确认状态字段
```

**初始化账号**（DataInitializer 首次启动自动创建，BCrypt）：
- 用户 `test / 123456`，管理员 `admin / admin123`

## 6 application.yml 配置

```yaml
server:
  port: 8081
spring:
  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:barter_db}...
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:root}
  servlet:
    multipart:
      max-file-size: 5MB
upload:
  path: ${UPLOAD_PATH:./uploads}
jwt:
  secret: ${JWT_SECRET:开发默认值}   # 生产必须更换
  expire: 86400000
ai:
  enable: ${AI_ENABLED:true}
  deepseek:
    base-url: ${DEEPSEEK_BASE_URL:https://api.deepseek.com}
    api-key: ${DEEPSEEK_API_KEY:}
    model: ${DEEPSEEK_MODEL:deepseek-chat}
```

## 7 架构约束

1. 分层：controller / service / mapper / entity / dto / vo / config / common
2. 统一返回 `Result<T>{code, msg, data}`，成功 code=200
3. 业务异常统一抛 `BusinessException`，全局异常处理器捕获返回友好提示
4. MyBatis 使用 XML 映射（`classpath:mapper/*.xml`），`map-underscore-to-camel-case` 开启
5. 关联展示字段拆分到 VO（IdleItemVO / ExchangeApplyVO），实体不混入关联字段
6. Controller 不直接注入 Mapper，统计一律 COUNT 查询
7. 前端请求统一走 `js/common.js` 封装（httpGet/httpPost/httpPut/httpDelete/uploadFile，自动携带 Bearer token，401 跳对应登录页）

## 8 验收标准

1. 按顺序执行 4 个 SQL 文件后表结构完整，应用启动自动创建测试账号
2. 用户发布物品后市场不可见，管理员审核通过后才展示
3. 管理员通过 `admin-login.html` 登录，用户登录页无法访问管理接口
4. 交换流程：申请→同意→约定时间地点（可反对循环）→双方确认→物品变已交换→可互评
5. 聊天、通知、收藏、举报功能全流程可用
6. `AI_ENABLED=false` 时 AI 返回模拟回答不报错
7. 所有图片通过文件选择上传，不可用 URL 直填

## 9 ⚠️ 已知简化（课堂项目范围）

- 忘记密码直接重置为 123456，无邮件/短信验证
- 聊天为 HTTP 轮询（5秒），非 WebSocket 长连接
- 图片上传至本地磁盘 `./uploads/`，未接对象存储
- 通知/聊天未做浏览器推送，需进入页面刷新查看
