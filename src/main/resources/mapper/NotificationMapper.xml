<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.barter.mapper.NotificationMapper">

    <resultMap id="BaseResultMap" type="com.barter.entity.Notification">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="type" property="type"/>
        <result column="biz_id" property="bizId"/>
        <result column="title" property="title"/>
        <result column="content" property="content"/>
        <result column="is_read" property="isRead"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <insert id="insert" parameterType="com.barter.entity.Notification" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO notification (user_id, type, biz_id, title, content, is_read)
        VALUES (#{userId}, #{type}, #{bizId}, #{title}, #{content}, 0)
    </insert>

    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT * FROM notification WHERE user_id = #{userId} ORDER BY create_time DESC LIMIT 100
    </select>

    <select id="countUnread" resultType="int">
        SELECT COUNT(*) FROM notification WHERE user_id = #{userId} AND is_read = 0
    </select>

    <update id="markAsRead">
        UPDATE notification SET is_read = 1 WHERE id = #{id} AND user_id = #{userId}
    </update>

    <update id="markAllAsRead">
        UPDATE notification SET is_read = 1 WHERE user_id = #{userId} AND is_read = 0
    </update>

</mapper>
