<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.barter.mapper.AiChatRecordMapper">

    <resultMap id="BaseResultMap" type="com.barter.entity.AiChatRecord">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="user_content" property="userContent"/>
        <result column="ai_content" property="aiContent"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <insert id="insert" parameterType="com.barter.entity.AiChatRecord" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO ai_chat_record (user_id, user_content, ai_content)
        VALUES (#{userId}, #{userContent}, #{aiContent})
    </insert>

    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT * FROM ai_chat_record WHERE user_id = #{userId} ORDER BY create_time ASC
    </select>

</mapper>
